void gInstall(char *name, int dataType, int size){
  globalVar[count]->dataType = dataType;
  strcpy(globalVar[count]->name, name);
  globalVar[count]->size = size;
  
}

gTable *gSearch(char *name){

}
